import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: '#0d47a1',
        borderRadius: 5,
        margin: 5,
        padding: 10,
    },
    text: {
        textAlign: 'center',
        color: 'white',
        fontWeight: 'bold',
    }
})